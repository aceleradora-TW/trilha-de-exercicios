package com.thoughtworks.aceleradora.oo.conversor;

public class Conversor {

    public String converter(String entrada) {
        int i=0;

        String moedaAtual = entrada.substring(0,2);
        String moedaFinal = entrada.substring(entrada.length()-3);
        //int valor = Integer.parseInt(entrada.substring(3,entrada.length()-4));

        for (i=2; i < entrada.length()-4;i++){


        }

       // switch (moedaAtual);

        return null;
    }
}
